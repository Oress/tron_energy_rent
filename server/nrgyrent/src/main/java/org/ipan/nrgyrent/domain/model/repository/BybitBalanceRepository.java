package org.ipan.nrgyrent.domain.model.repository;

import org.ipan.nrgyrent.domain.model.BybitBalance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BybitBalanceRepository extends JpaRepository<BybitBalance, String> {
}
